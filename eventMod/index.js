// const eventEmitter = require('events')

// const event = new eventEmitter();

// //First we have to declare our own event and
// event.on('sayMyName', () => {
//     console.log('My name is keshab')
// })
// event.on('sayMyName', () => {
//     console.log('My last name is kataruka')
// })
// event.on('sayMyName', () => {
//     console.log('My age is 20')
// })
// // then using emit we have to call it.
// event.emit('sayMyName')


//for my reference it is kind of Funtion define
// event.on("checkPage", (sc, msg) => {
    // console.log(`status code is ${sc} and the page is ${msg}`)
// })
// and this is kind of function call
// event.emit("checkPage", 200, "ok")